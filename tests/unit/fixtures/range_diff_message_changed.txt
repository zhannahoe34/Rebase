1:  0675f8d ! 1:  b397b38 Reject empty item names in restock
    @@ Metadata
     Author: Sandbox Bot <sandbox@example.com>
     
      ## Commit message ##
    -    Reject empty item names in restock
    -
    -    `restock` now raises if the item name is empty instead of creating a '' entry.
    +    Reject empty item names in restock (reworded)
     
      ## shop/inventory.py ##
     @@ shop/inventory.py: def restock(stock: dict[str, int], item: str, qty: int) -> dict[str, int]:
    @@ shop/inventory.py: def restock(stock: dict[str, int], item: str, qty: int) -> di
              raise ValueError("qty must be positive")
     +    if not item:
     +        raise ValueError("item name required")
    +     if qty > MAX_RESTOCK:
    +         raise ValueError(f"qty must be at most {MAX_RESTOCK}")
          stock[item] = stock.get(item, 0) + qty
    -     return stock
    - 
     
      ## tests/test_inventory_items.py (new) ##
     @@
